import React from 'react';
import HeaderContainer from '../../containers/HeaderContainer';

const App = (props) => {
  return (
    <div className="App">
      <HeaderContainer />
    </div>
  );
}

export default App;
